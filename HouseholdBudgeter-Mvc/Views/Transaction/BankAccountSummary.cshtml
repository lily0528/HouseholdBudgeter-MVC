@model List<HouseholdBankAccountDetailView>
@{
    ViewBag.Title = "BankAccountSummary";
}

<div class="card-header">
    <i class="fas fa-list-ul"></i>
    BankAccount List
</div>
<div class="card-body">
    <div class="table-responsive">

        @foreach (var bankAccount in Model)
        {

            <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0" bgcolor="green">
                <tbody>
                    <tr>

                        <td><h3>Account Name:  @bankAccount.Name</h3></td>
                        <td><h3>Blance:  @bankAccount.Balance </h3></td>
                    </tr>
                </tbody>
            </table>

                <table class="table table-bordered" id="dataTable" width="50%" cellspacing="0">
                    <thead class="card-header">
                        <tr>
                            <th>Id</th>
                            <th>Category Name</th>
                            <th>Amount</th>

                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var category in bankAccount.Categorys)
                        {
                            <tr>
                                <td>@category.CategoryId</td>
                                <td>@category.CategoryName</td>
                                <td>@category.CategoryAmount</td>
                            </tr>
                        }
                    </tbody>
                </table>
        }
    </div>
</div>



@*@model List<ViewBankAccountView>
    @{
        ViewBag.Title = "BankAccountDetails";
    }

    <div class="card-header">
        <i class="fas fa-list-ul"></i>
        BankAccount List
    </div>
    <div class="card-body">
        <div class="table-responsive">

            @foreach (var record in Model)
            {
                <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                    <tbody>
                        <tr>
                            <td>Account Id:  @record.Id</td>
                            <td>Account Name:  @record.Name</td>
                            <td>Blance:  @record.Balance</td>
                        </tr>
                    </tbody>
                </table>

                @Html.Action("TransactionDetails", "Transaction", new { id = record.Id })

            }

        </div>
    </div>*@